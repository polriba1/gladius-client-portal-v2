// Base64-encoded TTF font files for Inter Regular and SemiBold.
// Replace the placeholders with the actual base64 contents of your TTF files.
// Example to generate:
//   node: Buffer.from(fs.readFileSync('Inter-Regular.ttf')).toString('base64')
//   ensure you keep only the base64 string (no data: prefix)

export const interRegularBase64: string = ""; // TODO: insert Inter-Regular.ttf base64
export const interSemiBoldBase64: string = ""; // TODO: insert Inter-SemiBold.ttf base64
